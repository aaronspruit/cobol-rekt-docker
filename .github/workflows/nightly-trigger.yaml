name: trigger

on:
  workflow_dispatch:
  schedule:
    - cron:  '02 14 * * *'


jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - run: ./scripts/workflow-trigger.sh
